version: "3.9"

networks:
  proxy_network:
    name: proxy_network
    driver: bridge
  internal:
    driver: bridge

volumes:
  postgres_data:
  redis_data:

services:
  traefik:
    image: traefik:v3.6
    container_name: traefik
    restart: always
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    networks:
      - proxy_network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik:/letsencrypt
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=proxy_network"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.le.acme.httpchallenge=true"
      - "--certificatesresolvers.le.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.le.acme.email=ah167493991@gmail.com"
      - "--certificatesresolvers.le.acme.storage=/letsencrypt/acme.json"
      - "--api.dashboard=true"
      - "--api.insecure=true"

  postgres:
    image: postgres:15-alpine
    container_name: postgres
    restart: always
    networks:
      - internal
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - /opt/infra/env/database.env
    environment:
      POSTGRES_USER: postgres
      POSTGRES_DB: app_database
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: always
    networks:
      - internal
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # Example backend application service
  # Copy and customize this section for each of your applications
  # Replace 'your-app' with your actual app name throughout

  # your-first-app-backend:
  #   image: yourdockerhub/your-first-app-backend:latest
  #   container_name: your-first-app-backend
  #   restart: always
  #   env_file:
  #     - /opt/infra/env/your-first-app-backend.env
  #   networks:
  #     - proxy_network
  #     - internal
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.docker.network=proxy_network"
  #     - "traefik.http.routers.yourfirstapp.rule=Host(`your-first-app.example.com`)"
  #     - "traefik.http.routers.yourfirstapp.entrypoints=websecure"
  #     - "traefik.http.routers.yourfirstapp.tls=true"
  #     - "traefik.http.routers.yourfirstapp.tls.certresolver=le"
  #     - "traefik.http.services.yourfirstapp.loadbalancer.server.port=3000"

  # Uncomment and customize the above section for your applications
  # Each app gets its own service definition with unique:
  # - Image name (yourdockerhub/your-app-name-backend:latest)
  # - Container name (your-app-name-backend)
  # - Environment file (/opt/infra/env/your-app-name-backend.env)
  # - Domain (your-app-name.example.com)
  # - Router/service names (yourappname)
